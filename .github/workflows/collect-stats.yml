name: Collect Stats

on:
  workflow_dispatch:

concurrency:
  group: d3d-etl-collect-stats
  cancel-in-progress: false

jobs:
  collect-d1:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-chrome-${{ hashFiles('requirements.txt') }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          python -m playwright install --with-deps chrome

      - name: Install Xvfb
        run: |
          sudo apt-get update
          sudo apt-get install -y xvfb

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}
          role-session-name: d3d-etl-collect-stats-d1
          aws-region: ${{ secrets.AWS_REGION }}
          role-duration-seconds: 21600

      - name: Prepare output directory
        id: dirs
        env:
          DATA_ROOT: ${{ vars.DATA_ROOT }}
          NCAA_STATS_OUTDIR: ${{ vars.NCAA_STATS_OUTDIR }}
        run: |
          set -euo pipefail
          OUTDIR="${NCAA_STATS_OUTDIR:-${DATA_ROOT%/}/stats}"
          mkdir -p "$DATA_ROOT" "$OUTDIR"
          echo "outdir=$OUTDIR" >> "$GITHUB_OUTPUT"

      - name: Pull team history from S3
        if: vars.S3_BUCKET != ''
        env:
          TEAM_IDS_FILE: ${{ vars.TEAM_IDS_FILE }}
          S3_BUCKET: ${{ vars.S3_BUCKET }}
          S3_PREFIX: ${{ vars.S3_PREFIX }}
        run: |
          set -euo pipefail
          TEAM_IDS_FILE="${TEAM_IDS_FILE:-data/ncaa_team_history.csv}"
          mkdir -p "$(dirname "$TEAM_IDS_FILE")"
          aws s3 cp "s3://${S3_BUCKET}/${S3_PREFIX%/}/$(basename "$TEAM_IDS_FILE")" "$TEAM_IDS_FILE"

      - name: Scrape d1 stats
        env:
          YEARS: ${{ vars.YEARS }}
          TEAM_IDS_FILE: ${{ vars.TEAM_IDS_FILE }}
          OUTDIR: ${{ steps.dirs.outputs.outdir }}
          BASE_DELAY: ${{ vars.BASE_DELAY }}
          DAILY_BUDGET: ${{ vars.DAILY_BUDGET }}
          RANDOM_DELAY_MIN: ${{ vars.RANDOM_DELAY_MIN }}
          RANDOM_DELAY_MAX: ${{ vars.RANDOM_DELAY_MAX }}
        run: |
          set -euo pipefail

          if [ -z "${YEARS:-}" ]; then
            echo "ERROR: vars.YEARS is empty"
            exit 1
          fi

          IFS=',' read -r -a YEARS_ARR <<< "$YEARS"

          for YEAR in "${YEARS_ARR[@]}"; do
            xvfb-run -a python -u -m scrapers.collect_stats \
              --year "$YEAR" \
              --divisions 1 \
              --team_ids_file "${TEAM_IDS_FILE:-data/ncaa_team_history.csv}" \
              --outdir "$OUTDIR" \
              --base_delay "${BASE_DELAY:-10}" \
              --daily_budget "${DAILY_BUDGET:-20000}" \
              --random_delay_min "${RANDOM_DELAY_MIN:-1.0}" \
              --random_delay_max "${RANDOM_DELAY_MAX:-30.0}" \
              --run_all
          done

      - name: Upload d1 stats to S3
        if: always() && vars.S3_BUCKET != ''
        env:
          NCAA_STATS_OUTDIR: ${{ vars.NCAA_STATS_OUTDIR }}
          DATA_ROOT: ${{ vars.DATA_ROOT }}
          S3_BUCKET: ${{ vars.S3_BUCKET }}
          S3_PREFIX: ${{ vars.S3_PREFIX }}
          AWS_REGION: ${{ secrets.AWS_REGION }}
          S3_SSE_MODE: ${{ vars.S3_SSE_MODE }}
          S3_SSE_KMS_KEY_ID: ${{ vars.S3_SSE_KMS_KEY_ID }}
        run: |
          set -euo pipefail
          OUTDIR="${NCAA_STATS_OUTDIR:-${DATA_ROOT%/}/stats}"

          SSE_ARGS=""
          if [ -n "${S3_SSE_MODE:-}" ]; then
            SSE_ARGS="--sse $S3_SSE_MODE"
            if [ -n "${S3_SSE_KMS_KEY_ID:-}" ]; then
              SSE_ARGS="$SSE_ARGS --sse-kms-key-id $S3_SSE_KMS_KEY_ID"
            fi
          fi

          for f in "$OUTDIR"/d1_*; do
            [ -f "$f" ] && aws s3 cp "$f" "s3://${S3_BUCKET}/${S3_PREFIX%/}/stats/$(basename "$f")" $SSE_ARGS || true
          done

  collect-d2:
    needs: collect-d1
    if: always()
    runs-on: ubuntu-latest
    timeout-minutes: 360
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-chrome-${{ hashFiles('requirements.txt') }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          python -m playwright install --with-deps chrome

      - name: Install Xvfb
        run: |
          sudo apt-get update
          sudo apt-get install -y xvfb

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}
          role-session-name: d3d-etl-collect-stats-d2
          aws-region: ${{ secrets.AWS_REGION }}
          role-duration-seconds: 21600

      - name: Prepare output directory
        id: dirs
        env:
          DATA_ROOT: ${{ vars.DATA_ROOT }}
          NCAA_STATS_OUTDIR: ${{ vars.NCAA_STATS_OUTDIR }}
        run: |
          set -euo pipefail
          OUTDIR="${NCAA_STATS_OUTDIR:-${DATA_ROOT%/}/stats}"
          mkdir -p "$DATA_ROOT" "$OUTDIR"
          echo "outdir=$OUTDIR" >> "$GITHUB_OUTPUT"

      - name: Pull team history from S3
        if: vars.S3_BUCKET != ''
        env:
          TEAM_IDS_FILE: ${{ vars.TEAM_IDS_FILE }}
          S3_BUCKET: ${{ vars.S3_BUCKET }}
          S3_PREFIX: ${{ vars.S3_PREFIX }}
        run: |
          set -euo pipefail
          TEAM_IDS_FILE="${TEAM_IDS_FILE:-data/ncaa_team_history.csv}"
          mkdir -p "$(dirname "$TEAM_IDS_FILE")"
          aws s3 cp "s3://${S3_BUCKET}/${S3_PREFIX%/}/$(basename "$TEAM_IDS_FILE")" "$TEAM_IDS_FILE"

      - name: Scrape d2 stats
        env:
          YEARS: ${{ vars.YEARS }}
          TEAM_IDS_FILE: ${{ vars.TEAM_IDS_FILE }}
          OUTDIR: ${{ steps.dirs.outputs.outdir }}
          BASE_DELAY: ${{ vars.BASE_DELAY }}
          DAILY_BUDGET: ${{ vars.DAILY_BUDGET }}
          RANDOM_DELAY_MIN: ${{ vars.RANDOM_DELAY_MIN }}
          RANDOM_DELAY_MAX: ${{ vars.RANDOM_DELAY_MAX }}
        run: |
          set -euo pipefail

          if [ -z "${YEARS:-}" ]; then
            echo "ERROR: vars.YEARS is empty"
            exit 1
          fi

          IFS=',' read -r -a YEARS_ARR <<< "$YEARS"

          for YEAR in "${YEARS_ARR[@]}"; do
            xvfb-run -a python -u -m scrapers.collect_stats \
              --year "$YEAR" \
              --divisions 2 \
              --team_ids_file "${TEAM_IDS_FILE:-data/ncaa_team_history.csv}" \
              --outdir "$OUTDIR" \
              --base_delay "${BASE_DELAY:-10}" \
              --daily_budget "${DAILY_BUDGET:-20000}" \
              --random_delay_min "${RANDOM_DELAY_MIN:-1.0}" \
              --random_delay_max "${RANDOM_DELAY_MAX:-30.0}" \
              --run_all
          done

      - name: Upload d2 stats to S3
        if: always() && vars.S3_BUCKET != ''
        env:
          NCAA_STATS_OUTDIR: ${{ vars.NCAA_STATS_OUTDIR }}
          DATA_ROOT: ${{ vars.DATA_ROOT }}
          S3_BUCKET: ${{ vars.S3_BUCKET }}
          S3_PREFIX: ${{ vars.S3_PREFIX }}
          AWS_REGION: ${{ secrets.AWS_REGION }}
          S3_SSE_MODE: ${{ vars.S3_SSE_MODE }}
          S3_SSE_KMS_KEY_ID: ${{ vars.S3_SSE_KMS_KEY_ID }}
        run: |
          set -euo pipefail
          OUTDIR="${NCAA_STATS_OUTDIR:-${DATA_ROOT%/}/stats}"

          SSE_ARGS=""
          if [ -n "${S3_SSE_MODE:-}" ]; then
            SSE_ARGS="--sse $S3_SSE_MODE"
            if [ -n "${S3_SSE_KMS_KEY_ID:-}" ]; then
              SSE_ARGS="$SSE_ARGS --sse-kms-key-id $S3_SSE_KMS_KEY_ID"
            fi
          fi

          for f in "$OUTDIR"/d2_*; do
            [ -f "$f" ] && aws s3 cp "$f" "s3://${S3_BUCKET}/${S3_PREFIX%/}/stats/$(basename "$f")" $SSE_ARGS || true
          done

  collect-d3:
    needs: collect-d2
    if: always()
    runs-on: ubuntu-latest
    timeout-minutes: 360
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-chrome-${{ hashFiles('requirements.txt') }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          python -m playwright install --with-deps chrome

      - name: Install Xvfb
        run: |
          sudo apt-get update
          sudo apt-get install -y xvfb

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}
          role-session-name: d3d-etl-collect-stats-d3
          aws-region: ${{ secrets.AWS_REGION }}
          role-duration-seconds: 21600

      - name: Prepare output directory
        id: dirs
        env:
          DATA_ROOT: ${{ vars.DATA_ROOT }}
          NCAA_STATS_OUTDIR: ${{ vars.NCAA_STATS_OUTDIR }}
        run: |
          set -euo pipefail
          OUTDIR="${NCAA_STATS_OUTDIR:-${DATA_ROOT%/}/stats}"
          mkdir -p "$DATA_ROOT" "$OUTDIR"
          echo "outdir=$OUTDIR" >> "$GITHUB_OUTPUT"

      - name: Pull team history from S3
        if: vars.S3_BUCKET != ''
        env:
          TEAM_IDS_FILE: ${{ vars.TEAM_IDS_FILE }}
          S3_BUCKET: ${{ vars.S3_BUCKET }}
          S3_PREFIX: ${{ vars.S3_PREFIX }}
        run: |
          set -euo pipefail
          TEAM_IDS_FILE="${TEAM_IDS_FILE:-data/ncaa_team_history.csv}"
          mkdir -p "$(dirname "$TEAM_IDS_FILE")"
          aws s3 cp "s3://${S3_BUCKET}/${S3_PREFIX%/}/$(basename "$TEAM_IDS_FILE")" "$TEAM_IDS_FILE"

      - name: Scrape d3 stats
        env:
          YEARS: ${{ vars.YEARS }}
          TEAM_IDS_FILE: ${{ vars.TEAM_IDS_FILE }}
          OUTDIR: ${{ steps.dirs.outputs.outdir }}
          BASE_DELAY: ${{ vars.BASE_DELAY }}
          DAILY_BUDGET: ${{ vars.DAILY_BUDGET }}
          RANDOM_DELAY_MIN: ${{ vars.RANDOM_DELAY_MIN }}
          RANDOM_DELAY_MAX: ${{ vars.RANDOM_DELAY_MAX }}
        run: |
          set -euo pipefail

          if [ -z "${YEARS:-}" ]; then
            echo "ERROR: vars.YEARS is empty"
            exit 1
          fi

          IFS=',' read -r -a YEARS_ARR <<< "$YEARS"

          for YEAR in "${YEARS_ARR[@]}"; do
            xvfb-run -a python -u -m scrapers.collect_stats \
              --year "$YEAR" \
              --divisions 3 \
              --team_ids_file "${TEAM_IDS_FILE:-data/ncaa_team_history.csv}" \
              --outdir "$OUTDIR" \
              --base_delay "${BASE_DELAY:-10}" \
              --daily_budget "${DAILY_BUDGET:-20000}" \
              --random_delay_min "${RANDOM_DELAY_MIN:-1.0}" \
              --random_delay_max "${RANDOM_DELAY_MAX:-30.0}" \
              --run_all
          done

      - name: Upload d3 stats to S3
        if: always() && vars.S3_BUCKET != ''
        env:
          NCAA_STATS_OUTDIR: ${{ vars.NCAA_STATS_OUTDIR }}
          DATA_ROOT: ${{ vars.DATA_ROOT }}
          S3_BUCKET: ${{ vars.S3_BUCKET }}
          S3_PREFIX: ${{ vars.S3_PREFIX }}
          AWS_REGION: ${{ secrets.AWS_REGION }}
          S3_SSE_MODE: ${{ vars.S3_SSE_MODE }}
          S3_SSE_KMS_KEY_ID: ${{ vars.S3_SSE_KMS_KEY_ID }}
        run: |
          set -euo pipefail
          OUTDIR="${NCAA_STATS_OUTDIR:-${DATA_ROOT%/}/stats}"

          SSE_ARGS=""
          if [ -n "${S3_SSE_MODE:-}" ]; then
            SSE_ARGS="--sse $S3_SSE_MODE"
            if [ -n "${S3_SSE_KMS_KEY_ID:-}" ]; then
              SSE_ARGS="$SSE_ARGS --sse-kms-key-id $S3_SSE_KMS_KEY_ID"
            fi
          fi

          for f in "$OUTDIR"/d3_*; do
            [ -f "$f" ] && aws s3 cp "$f" "s3://${S3_BUCKET}/${S3_PREFIX%/}/stats/$(basename "$f")" $SSE_ARGS || true
          done
